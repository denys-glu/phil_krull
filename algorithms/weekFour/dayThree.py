# removeVal
# -----------------------------------------------------------------------------
# Create removeVal(list,value) that removes from our list the node with the given value. Return the new list. 1-2-3-4-5, 3 => 1-2-4-5

# prependVal
# -----------------------------------------------------------------------------
# Create prependVal(list,value,before) that inserts a listNode with given value immediately before the node with before (or at end). Return the new list. 1-2-4-5, 3, 4 => 1-2-3-4-5

# appendVal
# -----------------------------------------------------------------------------
# Create appendVal(list,value,after) that inserts a new listNode with given value immediately after the node containing after (or at end). Return the new list. 1-2-4-5, 3, 2 => 1-2-3-4-5


my_list = SLL()

def removeVal(node, val):
    node.next
    pass

removeVal(my_list.head.next.next.next, 3)

def removeVal(list, val):
    list.head.next
    pass

removeVal(my_list, 3)